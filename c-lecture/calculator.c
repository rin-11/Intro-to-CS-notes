// Operators and Types
#include <stdio.h>

int main(void)
{
    // Prompt user for x
    printf("x: ");
    int x;
    scanf("%d", &x);

    // Prompt user for y
    printf("y: ");
    int y;
    scanf("%d", &y);

    // Perform addition
    printf("%i\n", x + y);

    return 0;
} // 8